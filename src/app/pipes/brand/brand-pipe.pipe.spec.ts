import { BrandPipePipe } from './brand-pipe.pipe';

describe('BrandPipePipe', () => {
  it('create an instance', () => {
    const pipe = new BrandPipePipe();
    expect(pipe).toBeTruthy();
  });
});
