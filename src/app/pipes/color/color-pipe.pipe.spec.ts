import { ColorPipePipe } from './color-pipe.pipe';

describe('ColorPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ColorPipePipe();
    expect(pipe).toBeTruthy();
  });
});
